import { messages } from "@cucumber/messages";
import { IHook, Registry } from "./registry";
declare global {
    namespace globalThis {
        var __cypress_cucumber_preprocessor_dont_use_this: true | undefined;
    }
}
interface IStep {
    hook?: IHook;
    pickleStep?: messages.Pickle.IPickleStep;
}
export interface InternalProperties {
    pickle: messages.IPickle;
    testCaseStartedId: string;
    currentStep?: IStep;
    allSteps: IStep[];
    remainingSteps: IStep[];
}
export default function createTests(registry: Registry, source: string, gherkinDocument: messages.IGherkinDocument, pickles: messages.IPickle[], messagesEnabled: boolean, omitFiltered: boolean): void;
export {};
